@model IEnumerable<CurriculoOnLineMVC.Models.CandidatoPerfil>

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Details</h1>

<div class="card my-3">
    <div class="card-header">
        <h4>Candidato Perfil</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-0 ml-3">
                @Html.DisplayNameFor(model => model.Candidato.Id)
            </dt>
            <dd class="col-sm-1">
                @Html.DisplayFor(model => model.ToList()[0].Candidato.Id)
            </dd>
            <dt class="col-sm-0">
                @Html.DisplayNameFor(model => model.Candidato.Nome)
            </dt>
            <dd class="col-sm-2">
                @Html.DisplayFor(model => model.ToList()[0].Candidato.Nome)
            </dd>
        </dl>

        @*<table class="table">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Candidato.Nome)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Perfil.Descricao)
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Candidato.Nome)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Perfil.Descricao)
                        </td>
                        <td>
                            @Html.ActionLink("Edit", "Edit", new { id = item.CandidatoId }) |
                            @Html.ActionLink("Details", "Details", new { id = item.CandidatoId }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.CandidatoId })
                        </td>
                    </tr>
                }
            </tbody>
        </table>*@

        <div class="row">

            @{
                //var nome = string.Empty;
                var perfil = string.Empty;

                for (int i = 0; i < Model.ToList().Count; i++)
                {
                    //nome = Model.ToList()[i].Candidato.Nome;
                    perfil = Model.ToList()[i].Perfil.Descricao;

                    if(Model.ToList()[i].Candidato.Id == Model.ToList()[i].CandidatoId)
                    {
                    <div class="col-sm-3">
                        <!-- Default unchecked -->
                        <div class="custom-control custom-checkbox">
                            <input type="checkbox" class="custom-control-input" id="defaultChecked-@i" checked>
                            <label class="custom-control-label" for="defaultChecked-@i">@perfil</label>
                        </div>
                    </div>
                    }
        
                }
            }

        </div>

        <div class="mt-3 text-center">
            <button type="submit" class="btn btn-green mb-2 mr-2">Atualizar</button>
            <button type="submit" class="btn btn-green mb-2">Voltar</button>
        </div>

    </div>
</div>
    <div>
        @Html.ActionLink("Edit", "Edit", new { id = Model.ToList()[0].Candidato.Id }) |
        <a asp-action="Index">Back to List</a>
    </div>
